import 'package:flutter/material.dart';

class MyColors {
  static const Color primaryColor = Color.fromARGB(0, 168, 82, 255);  // Bright Blue
  static const Color accentColor = Color.fromARGB(0, 249, 140, 255);  // Light Pink
  static const Color secondaryColor = Color.fromRGBO(187, 73, 179, 0); // Purple
  static const Color infoColor = Color(0x2A51FF);     // Blue (can be used for info elements)
  static const Color textColor = Color.fromARGB(0, 134, 0, 223);  // Dark Purple (can be used for warnings)
  static const Color errorColor = Color(0xF52196);    // Red (can be used for errors)
  static const Color disabledColor = Color(0x691F75); // Dark Gray (can be used for disabled elements)
  //static const Color textColor = Colors.black;       // Pre-defined black for text

  // You can add more colors as needed following the same format
}